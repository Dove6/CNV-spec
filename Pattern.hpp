#include "object.hpp"

class PATTERN : OBJECT {
public:
    void ADD(string, integer, integer, string, integer);
    string GETGRAPHICSAT(integer, integer, bool, bool, integer);
    void MOVE(integer, integer);
};
